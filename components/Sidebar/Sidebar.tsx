import React from 'react'

import styles from './Sidebar.module.scss'

const Sidebar = (): React.ReactNode => {
  return <nav aria-label={'side navigation'} className={styles.sidebar}>

  </nav>
}

export default Sidebar
